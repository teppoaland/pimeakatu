# 🎯 Aktiivinen konteksti

> **Kevyt:** Vain tämä tiedosto luetaan session alussa.

---

## 📌 Kommunikaatiosääntö (KÄYTTÄJÄN PYYNTÖ)

> **"Lue membank"** → lue muistipankki hiljaa itseäsi varten. **ÄLÄ anna yhteenvetoa.** Muistipankki on Clinea varten, ei käyttäjälle raportoitavaksi. Käyttäjä on pyytänyt tätä useamman kerran.
>
> **🚫 Älä committaa automaattisesti** (20.9.2026) → jätä muutokset aina työpuuhun, jotta käyttäjä näkee ne VS Coden GIT-ikkunassa ja voi katsoa diffin. **Commit vain kun käyttäjä pyytää** (esim. "commit"). Push vain erillisestä komennosta.
>
> **✂️ Ei pitkiä yhteenvetoja** (20.9.2026) → *"en tarvitse yhteenvetoa kun muutoksia tulee niin paljon että en niitä ehdi kuitenkaan lukea"*. Raportoi 2–5 riviä: mitä muuttui + lopputulos. Ei taulukoita, ei vaiheittaisia selostuksia ellei erikseen pyydetä.

---

## 📍 Nyt

**v4.17 (nimi) – 🏷️ Pelinimi vaihdettu: `Boulder Däsh` → `Dig Däsh`** | 20.9.2026 | Käyttäjän pyyntö (*"Boulder Däsh => Dig Däsh"*). Kyse on **vain näkyvästä nimestä ja sen tekstiviittauksista** – mikään mekaniikka, tiedostonimi, polku tai tunniste ei muutu. **Muutettu (23 tekstikohtaa):** `digGame2/game_main.html:6` (`<title>`) + `:12` (näkyvä `<h1>💎 Dig Däsh</h1>`), `street.js:73` (`label: 'DIG\nDÄSH'`), `street.js:1288,1293` (kommentit), `gameState.js:18,22` (kommentit), `bm/js/audio.js:104` (kommentti), `PROJECT.md:27,28,53,65`, `README.md:23`, `.clinerules/01-general-architecture.md:32`, `.clinerules/02-game-core.md:55,56`, `docs/bd-cleanup.md:2`, `docs/bd-memo.md:1,7,93,98`, `docs/dig-game-memo.md:7,170`, `docs/plan.md:32,55`, `memory-bank/progress.md:25,67`, `memory-bank/systemPatterns.md:24,112`, `memory-bank/activeContext.md:111,124`. **Ei muutettu (tarkoituksella):** sisäiset tunnisteet `TILE.BOULDER` (26 osumaa), `boulderKeyCollected`, `BOULDER_KEY_COLLECTED`, `SCORE_BOULDER_FALL`, `drawBoulder`, kansionimi `digGame2/`, dokumenttitiedostot `docs/bd-memo.md` + `docs/bd-cleanup.md`, vanha build-tiedostonimi `boulder_dash.html` (`.clineignore`) ja git-historia (commit-viestit) – samat rajaukset kuin v4.17:n kaupallisten viittausten siivouksessa (`postMessage`-protokolla ja localStorage-avaimet ovat lukittuja, sääntö 02). **Käyttäjän linjaus 20.9.2026:** sana `boulder` tunnisteissa/muuttujissa **ei ole tavaramerkkiasia eikä sitä tarvitse muuttaa** – tunnisteet saavat jäädä sellaisinaan; ratkaisevat vain **näkyvät tekstit** (UI, otsikot, kommentit, dokumentit). **Ei versionostoa** – pelkkä tekstimuutos (sääntö 03) → `#version-tag` pysyy **v4.17:ssä**. **Validointi 20.9.2026 – 0 löydöstä:** grepit `Boulder Däsh|BOULDER DÄSH|Däshin|Däshistä|Däshiltä|Däshiin` → jäljellä vain historialliset lokimerkinnät (activeContext r. 18 ja progress r. 46, jotka kertovat *mitä v4.17:ssä tehtiin*), `Boulder Däsh`-otsikkoteksti 0 kpl käyttöliittymissä; `digGame2/game_main.html` title/h1 = `Dig Däsh`; avainportit ja `postMessage`-nimet ennallaan. Jos nimi halutaan myöhemmin myös tunnisteisiin (esim. `digDashKeyCollected`), se on **erillinen, luvan vaativa muutos** (localStorage-migraatio + pelien välinen API). **Git:** committattu `bb4fb27` ja pushattu `origin main`iin 20.9.2026 (käyttäjän komento "päivitä git" – sääntö 03).
**v4.18 – 🎵 Taustamusiikki: kappaleesta soi vain 30 s alku + 0,6 s häivytys** | 20.9.2026 | Käyttäjän pyyntö (*"soitetaankin vain 30s alusta ja sitten soitto katkeaa… laitan sitten jukeboxiin koko biisin"*). `audio.js`: **`PLAY_DURATION` (koko kappaleen kesto `loadedmetadata`ista) poistettu**; uudet vakiot `SONG_PLAY_LIMIT = 30000`, `SONG_FADE_OUT = 600`, `MUSIC_VOLUME = 0.05` + apurit `songPlayLength()` (`Math.min(SONG_PLAY_LIMIT, kesto + 1 s)` – turvaraja, jos tiedosto joskus vaihtuu lyhyemmäksi) ja `fadeOutSong()` (12 × 50 ms volume-ramppi → 0, sitten `silencePhase()`). `armPlayTimer(true)` = `setTimeout(fadeOutSong, songPlayLength() − SONG_FADE_OUT)` → **29 400 ms**; syntikka ennallaan (`SYNTH_PLAY_DURATION 30000`, `armPlayTimer(false)` → suoraan `silencePhase`). `loadedmetadata`-kuuntelija poistettu, `ended` jää turvaverkoksi, `fadeTimer`-siivous `silencePhase()`issa ja `stop()`issa, `stopMusicLoop()`/`startMusicLoop()` palauttavat `MUSIC_VOLUME`n. **Sykli: 30 s soittoa + 30–90 s tauko = 60–120 s** (ennen 158–218 s); kappale alkaa aina alusta (`currentTime = 0`). **Ei muutettu:** `getSilenceDuration()` (30–90 s), syntikka-fallback, autoplay-elelogiikka, `canplay`-vaihto kesken syntikan, musiikki soi alapelien aikana; **repotiedosto jää täydeksi 2:08 biisiksi** – tarkoitus on, että koko biisi tulee myöhemmin **jukeboxiin**. **`index.html` `#version-tag` → v4.18** (koodimuutos, sääntö 03) + `.clinerules/03-versioning.md` "Nykyinen versio" → v4.18. **Validointi:** `%TEMP%\audio-music-test.cjs` päivitetty → **43 tarkistusta, 0 löydöstä** (S1.4 ajastin 29 400 ms · S1.5–S1.6 häivytys ramppaa 0,05 → 0,0375 · S1.7 pysäytys + `currentTime = 0` + volumen palautus · S1.8 tauko 60 000 ms · S1.9 ei x2-toistoa · S1.11 uusi sykli alkaa alusta · S2 kesto NaN → SONG_PLAY_LIMIT · S3.2/S4.2 canplay & ele → 29 400 ms · S7.1 stop() siivoaa myös häivytyksen · S9.1 stop kesken häivytyksen · S9.2–S9.3 20 s tiedosto → 20 400 ms eikä ylitä kestoa) + `node --check audio.js`.

**v4.17 (jatko 2) – 🧹 Nimi yhdenmukaiseksi: `Blue Max` → `Blue Mäx` (ei-näkyvät tekstit)** | 20.9.2026 | Käyttäjän löydös `docs/plan.md:38` (*"Peli 3: Blue Max (`bm/`)"*) → koko repo tarkistettu. **Havainto:** nimi on jo vaihdettu kertaalleen tarkoituksella – commit `99be099` (v2.9, "nimi vaihdettu") muutti nimen täsmälleen samalla logiikalla kuin myöhemmin Boulder Däsh: `<title>`/`<h1>` `Blue Max` → `Blue Mäx`, canvas-intro `'BLUE MAX'` → `'BLUE MÄX'`, lamppulabel `'BLUE\nMAX'` → `'BLUE\nMÄX'`; `C64`-viite poistui v4.17:ssä (`dev`). **Siis jäljellä oli vain ilman ä:tä kirjoitettuja ei-näkyviä tekstejä 8 tiedostossa** → yhdenmukaistettu muotoon `Blue Mäx`: `docs/plan.md:38`, `.clinerules/01-general-architecture.md:33`, `PROJECT.md:31,58,66`, `docs/bm.md:1,7,13,157`, `bm/css/style.css:2`, `bm/js/audio.js:2`, `bm/js/game.js:2`, `street.js:1293` (vain kommentti; sääntö 01:n suojattu tiedosto – käyttäjän hyväksymä, sama rivi siivottiin jo v4.17:ssä) sekä `memory-bank/progress.md:76,80` + uusi progress-rivi. **Ei muutettu (tarkoituksella):** `const BlueMax` (`bm/js/game.js:4`), `window.BlueMax.init` (`bm/game_main.html:68`), `bmKeyCollected` (localStorage-rajapinta, sääntö 02), kansionimi `bm/`, tiedostonimi `docs/bm.md`, näkyvät `bm/game_main.html:6,12` ja `bm/js/game.js:138` (jo `Blue Mäx`, ei muutostarvetta). **Arvioitu erikseen eivät-kaupallisiksi (ei toimenpiteitä):** "Rule Britannia" (`bm/js/audio.js:3,205`, `docs/bm.md:139,158`) = perinteinen 1740-luvun laulu, public domain, soitetaan synteettisenä melodiana; "Sopwith Camel" (`bm/js/game.js:139`, `docs/bm.md:9`) = historiallinen WWI-lentokone (Sopwith Aviation lopetti 1920). **Tiedossa jäävä metadatajäänne:** git-commit-viestit mainitsevat yhä kaupallisia nimiä (`71b4247 Initial commit: Blue Max C64, Commando C64, digGame1/2, entrance hub` + vanhat Commando-viestit) – siivous vaatisi `git filter-branch --msg-filter` -uudelleenkirjoituksen (kaikki SHA:t vaihtuvat) + force pushin; **ei tehty** – **käyttäjän päätös 20.9.2026: metatiedot jätetään ennalleen** (*"ei tämä mikään kaupallinen/suuri tuote ole, ne ovat suunnitteluun liittyviä"*) → `--msg-filter`-siivousta ei tehdä eikä git-historiaa uudelleenkirjoiteta enää tästä syystä. **Ei versionostoa** – pelkät tekstit/kommentit/dokumentit (sääntö 03) → `#version-tag` pysyy **v4.17**. **Validointi:** skannaus `Blue Max|BLUE MAX` koko reposta (pois lukien `.git`) → **0 osumaa**; `node --check` `bm/js/game.js` + `bm/js/audio.js` OK; `git diff --stat` vain 8 tiedostoa.



**v4.17 (housekeeping) – 🧹 Äänimasterit repon ulkopuolelle + jukebox kirjattu** | 20.9.2026 | Biisien masterit (4 × `.mpeg`, ~23 MB) siirrettiin pois `docs/`-kansiosta → **`D:\AI\Knived`** (repon ulkopuolella; tarkoitus tulla peliin **jukeboxina**). `.gitignore`:iin uusi osio: `*.mpeg` ja `*.mp4` estetty (mastereita ei voi enää committaa vahingossa) – **`*.mp3` EI ole estetty**, koska pelissä soitettavat pakatut versiot kuuluvat repoon (GitHub Pages palvelee vain repon tiedostoja). Gitissä on äänistä vain `knived_unafraid.mp3` (2,0 MB, pelin taustamusiikki). **Muistipankki tarkistettu ja korjattu:** `systemPatterns.md` vastaa nyt todellista rakennetta (4 alipeliä: `digGame1`, `digGame2`, `bm`, `fruitgame`; ei buildattua `peli.html`:ää – buildaus poistettu v3.19; `bm/` kevyempi tiedostojoukko; portaalin juuressa `audio.js` + `knived_unafraid.mp3`; avaimet `digKeyCollected`/`boulderKeyCollected`/`bmKeyCollected` + Hedelmäpelin oma `pimeakatu_fruit_free`), `progress.md`-otsikko → v4.17 / 20.9.2026, ja tietograafi päivitetty (BlueMax → `bm/`, Commando merkitty poistetuksi, FruitGame lisätty, vanhentuneet "buildattu"-merkinnät poistettu). **Tila (päivitetty 20.9.2026):** kaikki on **pushattu** `origin main`iin – HEAD `bb4fb27` (pelinimi Dig Däsh), sitä ennen `65b826` (muistipankin tarkistus), `5a1146f`, `96a3ce9`, `8af0354`; työpuu puhdas, `main` == `origin/main` (0 ahead / 0 behind). Aiempi "lokaalisti pushaamatta" -merkintä on vanhentunut (force push tehtiin ja historia on siisti).


**v4.17 (jatko) – 🧹 Kaupallisten viittausten siivous: bändin nimi pois, `BOULDER DASH` → `Boulder Däsh`, `C64` → `dev`, `docs/boulder-dash-*.md` → `docs/bd-*.md`** | 20.9.2026 | **(1) "Iron Maiden" -viittaukset poistettu** kommenteista (`audio.js:3,45,348` + tämä muistipankki r. 101) → tilalla neutraali "Täysi bändisoundi" / "Laulumelodia". **`MELODY_NOTES` jää ennalleen** (käyttäjän päätös: syntikkamiksistä ei erota kappaletta) – soitto, `armPlayTimer`, MP3-fallback ja `SYNTH_PLAY_DURATION` koskemattomia. **(2) Havainto: `street.js`:n `lamps[].label` on KUOLLUTTA DATAA** – kenttää ei lueta missään (haku `\.label`/`dataset` ei osu), joten kadun lamppukyltit **eivät näytä pelien nimiä**; ainoa oikea kyltti on BAR (neon `fillText('BAR>')` r. 2821 + oven päällä vilkkuva `' BAR '` r. 3774). Siksi `'BOULDER\nDASH'` (r. 73) → `'BOULDER\nDÄSH'` on kosmeettinen, ja `COM-MANDO`/`BLUE MÄX`/`DIG GAME` eivät näy pelaajalle lainkaan (pimeässä huoneessa lukee vain "To be continued..."). **(3) "Boulder Dash" → "Boulder Däsh" 19 tekstissä:** `street.js:73,1288,1293`, `gameState.js:18,22`, `bm/js/audio.js:104`, `.clinerules/02-game-core.md:55,56`, `PROJECT.md:28`, `docs/bd-memo.md:7,98`, `docs/bd-cleanup.md:2`, `docs/dig-game-memo.md:7,170`, `memory-bank/progress.md:25,66`, `systemPatterns.md:105`, `activeContext.md:102,115`. **Ei muutettu:** tunnisteet `TILE.BOULDER` (26), `boulderKeyCollected`, `BOULDER_KEY_COLLECTED`, `SCORE_BOULDER_FALL` – sisäisiä nimiä, ei tuotemerkkiviittauksia. **(4) `C64` → `dev`:** `bm/game_main.html:12` (näkyvä HUD-tägi `dev`), `bm/css/style.css:2`, `bm/js/audio.js:2`, `bm/js/game.js:2` (otsikoista "Blue Max C64" → "Blue Max"), `docs/bm.md:7` ("C64-klassikosta" → "retroklassikosta"). Muistipankin `commando_c64`/`bluemax_c64`-kansionimet jätettiin (eivät ole Commodore-viittauksia). **(5) Dokumenttien nimet:** `git mv docs/boulder-dash-memo.md docs/bd-memo.md` + `git mv docs/boulder-dash-build-cleanup.md docs/bd-cleanup.md`; viittaukset korjattu (`PROJECT.md:29` linkki, `PROJECT.md:65` rakennekaavio, `CHANGELOG.md:16`, `docs/bd-cleanup.md:26`). **(6) Ei versionostoa** – muutokset ovat vain kommentteja, näkyviä tekstejä ja dokumentaatiota (sääntö 03) → v4.17 säilyy. **Validointi 20.9.2026 – 0 löydöstä:** grepit (`Iron Maiden|Running Free` 0, `Boulder Dash` 0, vanhat tiedostonimet 0, `C64` vain kansionimet) + **7 regressiotestiä läpi** (`audio-music-test` 31 tarkistusta, `street-bm-path`, `street-fruit`, `street-threshold`, `street-bar`, `street-avenger`, `street-win-frame`). **Avoimeksi jää:** `BLUE MÄX` -title screen pelin sisällä (`bm/js/game.js:138`), kuolleen `label`-datan kohtalo (`street.js:72–76`), `SOPWITH CAMEL - I MS` ja `"Rule, Britannia!"` (todettu turvallisiksi: lentokoneen nimi / public domain -sävelmä). **Force push `origin main`iin on edelleen tekemättä** (odottaa käyttäjän erillistä komentoa).


**v4.17 – 🧹 Siivous: commando_c64 pois git-historiasta + `bluemax_c64/` → `bm/` (+ `docs/bluemax.md` → `docs/bm.md`)** | 20.9.2026 | **(1) commando_c64 pois julkigitistä:** kansio oli jo poistettu työhakemistosta, mutta 4 tiedostoa olivat yhä `main`-haaran historiassa 8 commitissa → historia uudelleenkirjoitettu komennolla `git filter-branch --force --index-filter "git rm -r --cached --ignore-unmatch commando_c64" --prune-empty -- main`. Vain `main`-haara uudelleenkirjoitettiin → **Cline-checkpointit (`refs/cline/*`, 356 refiä) jäivät rauhaan**. `--prune-empty` pudotti 3 committia, jotka muuttuivat tyhjiksi (pelkät commando-commitit); 5 sekacommittia jäi historiaan, koska ne muokkaavat myös `index.html` / `memory-bank` / `.clinerules` -tiedostoja. **Tulos:** `main` 126 → **123 committia**, ja **sisältö säilyi identtisenä** – puun tiiviste ennen ja jälkeen = `1ce08c5…` (HEAD `ba1df02…` → `605dc93…`). Vanhat commit-SHA:t eivät enää osoita haaraan (historia uudelleenkirjoitettu, kaikki SHA:t vaihtuivat). Paikalliset `refs/cline/checkpoints/*` (356 kpl) pitävät vanhat objektit yhä `.git`issä – vain paikallisesti (8 committia löytyy `git log --all` -haulla); julkinen git puhdistuu force pushilla. Alkuperäisen commitin *viesti* mainitsee nimen (metatieto, ei tiedosto). Siivous: `refs/original/*` poistettu, `git reflog expire --expire=now --all`, `git gc --prune=now`. Tekstiviittaukset poistettu tiedostoista `README.md`, `PROJECT.md` (Commando-osio), `CHANGELOG.md` (kaksi poistoriviä) ja `.clinerules/01-general-architecture.md` (taulukkorivi + koko 🚫-osio). **Portaalin lukitut tekstit jätettiin ennalleen:** `street.js`-kommentti "(Pokaali / COMMANDO)" ja `COM-\nMANDO`-label ovat palkintohuoneen pelisisältöä, eivät viittaus poistettuun kansioon. **(2) Alipelikansion uudelleennimeäminen:** `git mv bluemax_c64 bm` ja `git mv docs/bluemax.md docs/bm.md` (git tunnistaa `R`-nimityksiksi; pelin sisällä ei ole kansiopolkuun sidottuja avaimia – ainoa localStorage-avain on portaalin `pimeakatu_gamestate` → tallennettu tila säilyy). Viittaukset korjattu: `street.js:74` `gameUrl: 'bm/game_main.html'` ja **avainportti r. 1294** `lamp.gameUrl.includes('bluemax')` → `lamp.gameUrl.includes('bm/')` (tarkoituksella kauttaviivalla, jotta ei osu digGame1/digGame2-polkuihin) sekä `.clinerules/01` (taulukko + puu), `README.md`, `PROJECT.md` (osio + lisätietolinkki + rakennekaavio + docs-lista), `docs/plan.md`, `docs/bm.md` (rakennekaava), `memory-bank/progress.md` ja `memory-bank/systemPatterns.md` (kaaviolaatta). Pelin otsikot ("Blue Max — Memo", `✈️ Blue Mäx`) säilyivät – kyse on polusta, ei pelin nimestä. **(3) Samalla committoitiin keskeneräinen v4.16-työ:** `knived_unafraid.mp3` lisätty versionhallintaan, vanhat `running.mp3` ja `docs/running.mpeg` poistettu; `docs/Knived_unafraid.mpeg` (3,9 MB arkisto) jää version ulkopuolelle. `#version-tag` → **v4.17**. **Validointi 20.9.2026 – 0 löydöstä** (ks. alla: `%TEMP%\street-bm-path-test.cjs`, HTTP-savutesti, `git log/rev-list/ls-files` -tarkistukset). Force push `origin main`iin **ei tehty** – odottaa käyttäjän erillistä komentoa (sääntö 03).


**v4.16 – 🎵 Taustamusiikki vaihdettu: Knived Unafraid (2:08) + kertasoitto ja kestopohjainen ajastin** | 20.9.2026 | `audio.js`: vanha 15,5 s `running.mp3` korvattu käyttäjän omalla teoksella **`knived_unafraid.mp3` (128,05 s = 2:08)**. Koska kappale on pitkä, **x2-toiminto poistettu**: `ended` ei enää soita toista kertaa (`musicPlayed === 1` -suoja) vaan siirtyy suoraan taukoon (`silencePhase()`), joka on edelleen 30–90 s satunnainen; myös `gapTimer` (3 s tauko looppien välissä) poistettu. `PLAY_DURATION = kesto + 1 s` (kertasoitto + häntä). **Uusi apuri `armPlayTimer(useSong)`**: soittovaiheen ajastin luetaan aina `musicEl.duration`-arvosta (varalla `PLAY_DURATION`, syntikalle uusi `SYNTH_PLAY_DURATION = 30000`) ja se kutsutaan `playPhase()`issa, `canplay`-vaihdossa ja `onGesture`-uudelleenyrityksessä → **korjaa aikakilpailun**, jossa 30 s oletus olisi katkaissut 2:08-kappaleen, jos metatiedot eivät ole latautuneet ennen ensimmäistä elettä (vanhalla 2 × 15,5 s + 3 s sattui olemaan ~31 s). **Äänitiedosto:** `ffmpeg -af volume=-7dB -ar 44100 -ac 2 -b:a 128k` → 3,91 MB / 256 kbps → **2,00 MB / 128 kbps**; loudness −5,6 → **−13,0 LUFS** (vanha oli −12,6 LUFS) eli `musicEl.volume = 0.05` säilyy koskemattomana ja kuuntelutaso on sama; true peak +1,7 → **−4,8 dBTP** (lähdetiedostossa oli jo yli 0 dBFS -piikkejä). Vanhat `running.mp3` ja `docs/running.mpeg` **poistettu**; biisien masterit on siirretty repon ulkopuolelle (`D:\AI\Knived`) ja `.gitignore` estää `*.mpeg`/`*.mp4`-committaukset (ks. jukebox-merkintä). Synth-fallback (autoplay-esto), `stop()`, kuolinääni ja "musiikki jatkuu alapelien aikana" ennallaan. `#version-tag` → **v4.16**. **Validointi 20.9.2026 – 0 löydöstä** (`%TEMP%\audio-music-test.cjs`, 31 tarkistusta, ei repossa – sama Node `vm` + stub-tekniikka kuin `street-*-test.cjs`): staattisesti (tiedostonimi, ei `gapTimer`ia / `duration * 2`:ta / `, 3000)`-ajastinta, `armPlayTimer` 3 + 2 kutsua, `musicPlayed === 1` -suoja) ja käyttäytymisestä fake-kellolla + `<audio>`/`AudioContext`-stubeilla: ensimmäinen ele soittaa **kerran** ja ajastin = 129 052 ms (kesto + 1 s), kappaleen päätyttyä suoraan taukoon (60 000 ms), **3 s kuluttua ei tule toista soittoa**, tupla-`ended` ei tuplaa, tauon jälkeen uusi kertasoitto, keston puuttuessa 30 s varasuoja, `canplay` vaihtaa kappaleeseen kesken syntikan (ajastin 30 s → 129 s), autoplay-esto → syntikka 30 s + eleestä kappale takaisin, `ended` tauon aikana ei tee mitään, tauko 30 000 / 60 000 / 90 000 ms raja-arvoilla, `stop()` nollaa ajastimet, `touchstart` käynnistää. Lisäksi: mp3 **dekoodautuu virheittä** ja HTTP-palvelin (8080) tarjoilee `knived_unafraid.mp3` → 200 / `audio/mpeg` / 2 049 389 tavua, kun taas `running.mp3` → 404.


**v4.15 – 💥 Oviukon isku dramaattisemmaksi: kontakti jäädyttää koko maailman 3 s, sitten pelaaja kosahtaa kasaan** | 19.9.2026 | Käyttäjän havainto: *"ukko tulee talosta hienosti, mutta se vain vilahtaa – saisi olla dramaattisempi 3 s jäätyminen ja sitten pelaaja kosahtaa kasaan"*. `street.js`: oviukon aikajanalle uusi tila **`hold`** (`emerge → chase → hold → kosahtaminen → return`). Kontaktissa (`d < AVENGER_HIT_R`) ei enää tainnuteta heti, vaan `a.phase = 'hold'` + **`hitPauseTimer = AVENGER_FREEZE` (180 f = 3 s)** → koko maailma jäätyy (sama hit-stop kuin potkuosumissa: `update()` palauttaa heti, render jatkaa). Vasta hit-stopin kuluttua `hold`-haara ajaa `knockPlayerDown()` (tainnutus 600 f + −1 🍔) ja oviukko lähtee paluuseen. **Draama renderissä:** 1–2 px vaakasuuntainen tärinä (`hitPauseTimer` toimii kellona, koska `animClock` on jäässä), **iskuvälähdys** (lämmin valkoinen, haihtuu jäädytyksen ensimmäisellä 10 %:lla), tummeneva **vinjetti** (`createRadialGradient`, 0 → 0.6 alpha) ja **tähdet alkavat kiertää pään ympäri** jo ennen kosahtamista (`AVENGER_FREEZE − hitPauseTimer` -kello). Oviukon käsi on ojennettuna kohti pelaajaa `hold`-vaiheessa (`drawAvenger`). Säädönuppi `AVENGER_FREEZE = 180` (120 = 2 s, 300 = 5 s). `#version-tag` → **v4.15**. **Validointi 19.9.2026 – 0 löydöstä** (`%TEMP%\street-avenger-test.cjs`, nyt 28 tarkistusta): kontaktissa `phase=hold` eikä pelaaja vielä kosahtanut (🍔 6), **pelaaja ei liiku vaikka juoksee** ja oviukko seisoo paikallaan 60 f (maailma jäässä), jäädytys kesti 181 f (≈180 = 3 s), vasta sitten tainnutus 600 f + tasan 1 🍔, ei tuplaosumaa, paluu ovelle `null`, iframe-jäädytys, suoja valmiiksi tainnoksissa, 🍔 1→0 → kuolema, regressiot rnd 0.5/0.15 ennallaan, cooldown + portti ennallaan. Regressiot 0 löydöstä: `street-fruit-test.cjs`, `street-bar-test.cjs`, `street-threshold-test.cjs`, `street-winframe-test.cjs`.

**v4.14 – 🚪 Oviukko (Avenger): kolikon vastakohta – tulee ovesta, tainnuttaa (ei väistettävissä)** | 19.9.2026 | `street.js`: potkun pudotus sai **kolmannen arvonnan**, joka on kolikon peilikuva: `spawnKickDrop()` → **1/8** (`AVENGER_CHANCE 0.12`) + **30 s cooldown** → **oviukko**, muuten vanha logiikka (1/5 kolikko → kukkaruukku). **Ei putoa ikkunasta:** `spawnAvenger(bldg)` asettaa hahmon kynnykselle (`doorCenter(bldg).x − 10`, jalat `GROUND_Y` → y 280; ruukun lähtökorkeus on 210) ja `buildingScale(bldg)`-skaalaan (talo 0 = 100 %, talot 2/4 = 90 %). Ukkeli on pelaajan kaksonen samalla blokkityylillä kuin `drawPlayer()`, mutta tunnisteväri tummanpunainen paita `#a03030` / hiha `#7a2020` (pelaaja `#3366cc`) → erottuu ilman tekstiä. **Aikajana:** `emerge` (21 f ≈ 350 ms oven varoitus, hahmo liukuu esiin kynnykseltä) → `chase` (`AVENGER_SPEED 2.0` px/f > pelaajan 1.225 → **tavoittaa aina**, sekä x että y; `AVENGER_HIT_R 18`) → osuma → `return` (kävelee takaisin kynnykselle ja katoaa, puff). **Osuma** = uusi apuri `knockPlayerDown()`: tainnutus **600 f** (`AVENGER_STUN`, sama kuin ruukulla) + **−1 🍔** + `killPlayer()` jos 0; suoja `if (!player.knockedDown)` → ei tuplavähennystä. Uusi ääni `playKnock()` (kohina + 150→45 Hz sine). **Kutsut:** `updateAvenger(dt)` ajetaan sekä normaalivirrasta (kickCoin-lohkon jälkeen) että tainnutus-haarasta (smallHouseLights jälkeen) → paluuanimaatio jatkuu kun pelaaja makaa maassa; **iframen aikana jäissä** (`#game-iframe-overlay.active`) → ei näkymätöntä osumaa kesken pelin. Portit: `&& !avenger` molempiin ovihaaroihin (talo 0 ja pienet talot) → ei ruukkua/kolikkoa/ukkoa samaan aikaan. `drawAvenger()` piirretään juuri ennen `drawPlayer()` (pelaaja päällimmäisenä). `#version-tag` → **v4.14**. **Ei uusia tekstejä/dialogeja** (tapahtuma näkyy visuaalisesti: ovi välähtää → ukko astuu ulos → tähdet pään päällä). **Validointi 19.9.2026 – 0 löydöstä** (`%TEMP%\street-avenger-test.cjs`, Node `vm` + canvas/document-stub + `_dbg`-export vain testikopiossa, ei repossa; 23 tarkistusta): oviukko syntyy kynnykseltä y 280 (≠ ruukku 210), täydellä nopeudella pakeneva pelaaja tavoitetaan silti 33 f kohdalla (etäisyys 25,2 → 15,8), osuma = 600 f + tasan 1 🍔 (tallennettu), 120 f tainnutuksen aikana ei tuplaosumaa, oviukko palaa ovelle ja `avenger = null`, tainnutus päättyi (~600 f) ja 🍔 pysyi 5:ssä, iframe auki = jäissä 60 f, valmiiksi tainnoksissa ei toista −1 🍔, 🍔 1→0 → kuolemasekvenssi, regressio rnd 0.5 → ruukku ja rnd 0.15 → kolikko ennallaan, cooldown estää toisen oviukon (sama olio) ja kuluu frameissä (1770 → 1710), portti `!avenger` esti pudotuksen. Regressiot 0 löydöstä: `street-fruit-test.cjs`, `street-bar-test.cjs`, `street-threshold-test.cjs`, `street-winframe-test.cjs`. **Seuraavaksi mahdollisesti:** koira toisena vaihtoehtona 70/30-jaolla (`AVENGER_KIND`) – vain jos ukko toimii hyvin pelitestissä.

**v4.13 – 🚪 Ovien kynnykset: kynnyslinja + laatta + yksi pieni kivirivi + laskettu reunakivi** | 19.9.2026 | `street.js`: jokaisen 9 oven eteen tuli kynnysdetalji. **Kynnyslinja** (2 px: `#3a3a3a` + `#1c1c1c`) + **kynnyslaatta** (5 px, `GROUND_Y+2…+6`) + **yksi kivirivi** heti laatan vieressä (`THRESH_TOP_Y 7`, `THRESH_RINGS [{ d: 5, w: 16, n: 3 }]`) + **laskettu reunakivi**: reunakivien (`splitAtKerbGaps`) ja kahden kadun viivan (`drawStreetLine`) katko oviaukon kohdalta ±(ovi/2+5) sekä 2 px viisteet katkon reunoille (`#343434` + `#0b0b0b`). Kivirivin saumat ovat vinottain kohti kynnystä (u·w-skaalaus), V-painuma 2 px (`THRESH_DIP`), kulunut keskikivi (`wear`, shade −2,5), 2 halkeamaa saumaa pitkin ja 2 pikkukiveä (väistävät viemärinkannet 15 px). **Kynnysvalo** vain "aktiivisille" oville (sama logiikka kuin `drawDoor`: talon lamppu palaa; BAR idx 8 aina) ja klipattuna kivirivin ulkoreunaan: talot `rgba(255,230,150,…)`, BAR pinkki `rgba(255,102,163,…)`. **Kovat rajat:** kiveys `GROUND_Y+7…+14` (y 317–324, s=1), puolileveys ovi+5 → autokaistat (y 328/340) jäävät vapaiksi. Lisäksi **E:** kiveysrivien saumavaihe ja sävy vaihtelevat taloittain (`housePhase` 0–6 px, `houseDrift` −1…+1, deterministinen LCG per talo) → sama ruudukko ei jatku "barina" laidasta laitaan. `#version-tag` → v4.13. **Korjaushistoria:** ensimmäinen toteutus oli 4 rengasta / 35 px syvä viuhka (alaraja y 350 asti) → käyttäjän havainto 19.9.2026: *kiveys valui autotielle ja oli 100 % ristiriidassa vaakakiveyksen kanssa, koska talot ovat kiinni kadun varressa* → viuhka kutistettiin **yhteen riviin (35 px → 5 px)** ja siirrettiin kynnyslaatan alle; kynnyslaatta palautettiin alkuperäiseen 5 px korkoon ("muu pysyy samana"). **Validointi 19.9.2026 – 0 löydöstä** (`%TEMP%\street-threshold-test.cjs`, Node `vm` + canvas-stub + `_dbg`-export vain testikopiossa, ei repossa): 9/9 ovea; kiilat peittävät rivin aukottomasti ja ilman päällekkäisyyttä (0,5 px pistetesti + saumatoleranssi); **kovat rajat** – alaraja ≤ `GROUND_Y+14`, ei koskaan kaistalle y 328, puolileveys ≤ ovi+5, ei osumia lamppujen jalustoihin/tolppiin; kadun viivan katko (peitto 486 px), 24 reunakiveä, 18 viistettä, 9 kynnyslaattaa; kynnysvalo pimeällä 1 (BAR) ja kaikkien lamppujen palaessa 5 + sävyt; kiveysrivit ennallaan (141 kiveä, 4 riviä, sävyt 10–17) ja vaiheistus näkyy 38/137 saumassa. Regressiot: `%TEMP%\street-fruit-test.cjs`, `%TEMP%\street-bar-test.cjs`, `%TEMP%\street-winframe-test.cjs` → 0 löydöstä.


**v4.12 – 🍔 BAR-ostokset: ostomäärä nuolilla (▲ osta 1 / ▼ peru 1) + (o) poistuu** | 19.9.2026 | Vanha kertapainallus ("osta maksimit ja poistu") poistettu. `street.js`: uudet tilat `barBuyQty` (vierailun ostot), `barBuyHeldUp`, `barBuyHeldDown` (reunanilmaisu → ei toistoa pohjassa). BAR-haara (`update()`, ~r. 622–660): **▲ / W** = osta 1 (1 kolikko, katto 10, ei osta ilman kolikoita) · **▼ / S** = peru viimeisin osto (kolikko takaisin, **vain tämän vierailun ostot** → ei rahareikää) · **(o) / Space / Enter** (`actionJustPressed`) = poistu. Vierailun alkaessa (`barRoom = true`) ja poistuessa `barBuyQty`/reunaliput nollataan → uusi vierailu alkaa nollasta. Mobiili: D-pad ▲/▼ = `keys['ArrowUp'/'ArrowDown']` (setupBtn), ⚡ `action-btn` = poistu; PC: ↑/W, ↓/S ja Space/Enter. `drawBarRoom()`: tilateksti näyttää `Ostit Xx🍔 hampurilaista!` (X = `barBuyQty`, pienenee ▼:llä), muuten nykyiset lukitut tilat (kiintiö täynnä / ei kolikoita); alaohje päivitetty (lupa pyydetty ja saatu): `▲ = osta 1 🍔   ▼ = peru 1   POISTU: (o) / Space` (aiempi kaksirivinen Space-ohje poistettu). `#version-tag` → v4.12. **Validointi 19.9.2026 – 0 löydöstä** (`%TEMP%\street-bar-test.cjs`, Node `vm` + canvas/document-stub + DOM-nappien listener-rekisteri, ei repossa): 18 tarkistusta läpi – huone aukeaa BAR-ovelta (talo 8, ovi x 765) niin näppäimistöllä kuin pelkillä kosketusnapeilla (btn-down/right/up + action-btn); ▲ ostaa tasan 1; **▲ pohjassa 90 frameä = edelleen 1 osto**; ▼ peruu yhden ja pysähtyy kun vierailun ostot on palautettu (kolikot/burgerit täsmälleen lähtötilassa, ei "myyntiä"); osto pysähtyy kattoon 10 (ja kolikoiden loppuessa), lisä-▲ ei osta yli; Space/(o) poistuu ostamatta; kadulla ▲ ei vaikuta saldoon; uusi vierailu alkaa nollasta (vanhoja ostoja ei voi perua); mobiilissa synteettinen mousedown ei tuplaa ostoa (400 ms suoja) ja ▲/▼ rekisteröityvät touchista. Regressio: `%TEMP%\street-fruit-test.cjs` ajettu uudelleen (hedelmäpeli-kytkentä ehjä).

**v4.11 – 🍒 Hedelmäpeli kytketty katuun + talon ilmainen pyöräytys** | 19.9.2026 | **(1) Vaihe 2 (katu-kytkentä):** `street.js` `handleAction()` sai oman oviblokin **talo 7:lle** (`buildings[6]`, ovi x 585) **ennen** lamppusilmukkaa → `enterGame('fruitgame/game_main.html')`, aina auki (ei lamppua eikä avainta) → ohittaa talon potku- ja 1/5-kolikkohaaran. `enterGame()`in `iframe.onload` lähettää `fruitSync`in uuden apurin `sendFruitBalance()` kautta (+ fokusoi iframen); `window._streetReturn` sai haarat `fruitBet` (−1, clamp ≥ 0) ja `fruitWin` (+n, `floor` + clamp) → `GameState.save(state)` + `updateHUD()` + **saldo-echo peliin**. Vanhat viestit (`RETURN_TO_STREET`, `KEY_COLLECTED`, `BM_KEY_COLLECTED`, `BOULDER_KEY_COLLECTED`, `COIN_COLLECTED`) ennallaan; `coinCount` pysyy kadun autoritatiivisena. **(2) Talon ilmainen pyöräytys:** sisään tullessa 1 ilmainen ja **voiton saa pitää**; jäädytys **120 s** (`FREE_SPIN_COOLDOWN_MS`, `FREE_SPIN_KEY = 'pimeakatu_fruit_free'` pelin omassa localStoragessa → kadun `inventory`/`pimeakatu_gamestate` **koskematon**); paluu alle 120 s → jokainen pyöräytys maksaa 1 kolikon (farmaus estetty, max +0,785 kolikkoa / 2 min ≈ +0,39/min). Ilmainen ei veloita eikä lähetä `fruitBet`iä (`sendToParent` ei kutsuta), jäädytys käynnistyy vasta kulutuksesta. Uusi `AudioFX.playFreeSpin()` (kirkas nousu), canvas-merkki `ILMAINEN` + jäädytyslaskuri `m:ss` (`Renderer.drawFreeSpin`, x 38 y 108) ja napin `free`-tila (vihreä hehku; `updateHUD` ei disabloi nappia ilmaisen aikana). Uudet viestit: `Talo tarjoaa 1 ilmaisen pyöräytyksen!`, `Talo tarjoaa: ilmainen pyöräytys!`, `Ilmaisen voitto: +N kolikkoa!`, `Ei kolikoita! Ilmainen pyöräytys m:ss kuluttua.` **(3) Validointi – 0 löydöstä:** `%TEMP%\ftest.cjs` standalone + iframe (T1–T14: talous/HUD/rullat/maksut 125/125/RTP 78,5 %/dt/skaalaus/ohjaus/protokolla/ilmainen) ja **uusi `%TEMP%\street-fruit-test.cjs`** (kävely talo 7:n ovelle 4 siemenellä → ovi aukeaa, `fruitSync` 5 → `fruitBet` 4 → `fruitWin` 11 → clamp 0 → `RETURN_TO_STREET` tyhjentää iframen ja lopettaa protokollan; `COIN_COLLECTED` ennallaan; muut ovet eivät avaa hedelmäpeliä). Versio v4.10 → **v4.11** (`index.html`), **`docs/fruit-game-memo.md` luotu**. **Ei toteutettu (vaatii luvan):** nälkäajastimen jäädytys iframen ajaksi (`street.js`) – nyt nälkä tikittää myös pelin aikana → näkymätön kuolema / reload kesken pyöräytyksen. **Hylätty:** "1 kolikko = 2 pyöräytystä" (RTP/kolikko olisi 157 % → rahalähde).

**🍒 Hedelmäpeli – DEBUG-KIERROS 1 (standalone, iframe-protokolla validoitu)** | 19.9.2026 | Ajettu headless-validointi (**`%TEMP%\ftest.cjs`** – Node `vm` + canvas/document-stub, rAF käsin ohjattuna, **ei repossa**) sekä standalone- että iframe-tilassa. **Löydös 1 (KRIITTINEN, korjattu):** `new FruitGame()` puuttui kokonaan → peli ei käynnistynyt lainkaan (tyhjä canvas, ei kuuntelijoita; Vaihe 1:n smoke-testi loi instanssin käsin, siksi jäi huomaamatta). Lisätty `fruitgame/js/game.js`:n loppuun `window.addEventListener('DOMContentLoaded', () => { new FruitGame(); });` (sama käytäntö kuin digGame1/digGame2). **Löydös 2 (kosmeettinen, korjattu):** globaali `* { box-sizing: border-box }` + 3 px kehys kutisti `#game-canvas`-piirtoalueen 634×394:ään bittikartan ollessa 640×400 (epätasainen pikseliskaalaus; `resize()`:n `-6` oli tarkoitettu juuri kehykselle) → `fruitgame/css/style.css`: `#game-canvas { box-sizing: content-box }`. **Läpi menneet (0 löydöstä, 800 pyöräytystä):** saldo/HUD/spins/wins/nappi-disabled täsmäävät joka pyöräytyksellä; rulla pysähtyy aina tarkalleen `resultSlots[i]`-ruutuun (`pos mod nauha`) ja ruudun symboli == `resultIds[i]`; maksutaulukko 125/125 (5 kolmikkoa / 40 paria / 80 nollaa, symmetria); RTP sim 78,5–78,8 % vs. analyyttinen **78,49 %** (kolmikko 6,85 %, pari 35,30 %); `distToTarget` > 0 aina ja maali osuu oikeaan ruutuun; jarrutus dt-riippumaton (dt 1/144…0,06) eikä ylitä tavoitetta; **0 piirtoa kankaan ulkopuolelle** (25 renderöityä pyöräytystä + pakotettu jättipotti + pisin viesti 11 px/merkki -mitoituksella); `resize()` 5 ikkunakoossa (kuvasuhde 1,6 säilyy); ohjaus Space/Enter/Esc/Backspace + kanvas (touch/mouse + 400 ms synteettinen-mousedown-suoja) + napit; protokolla 40×`fruitBet` + `fruitWin`-summa == voitorivit, `fruitSync` päivittää saldon/HUDin (clamp + pyöristys), `RETURN_TO_STREET` estyy kesken pyörinnän, standalone ei lähetä viestejä. **Huomiot (ei muutettu):** (a) kierroksen kesto **1,40–2,80 s (ka 2,26 s)** johtuu "etsi tavoite 4 symbolin sisään" -logiikasta → säätönupit `STOP_DELAYS`/`DECEL_DIST`/`SPIN_SPEED`; (b) `distToTarget`in "vähintään 1 symboli" -sääntö (`if (z <= pos + 1) z += L`) siirtää maalin täyden kierroksen päähän (max d = 21, ~5 % tapauksista) → yksi rulla pyörii ~1,33 s pidempään; esteettinen, ei virhe. **index.html EI koskettu** → versio pysyy **v4.10**; Vaihe 2 nostaa sen v4.11:een suunnitelman mukaan (jos haluat version nousevan jo tästä, pyydä erikseen). **Seuraavaksi: Vaihe 2 – kytkentä (ks. "Seuraavaksi").**

**🍒 Hedelmäpeli – VAIHE 1 VALMIS (standalone, ei vielä kytketty)** | 19.9.2026 | Uusi alapeli `D:\AI\Main\fruitgame\`: `game_main.html` + `css/style.css` + `js/constants.js` (symbolit/painot/maksut/animaatiovakiot), `js/audio.js` (`AudioFX`), `js/renderer.js` (canvas-kone + 5 käsipiirrettyä symbolia), `js/input.js`, `js/game.js` (tila, RNG, rullafysiikka, postMessage-silta). **Pääportaaliin ei koskettu** (index.html/street.js ennallaan, versio pysyy v4.10:ssä, `fruitgame/` on untracked Gitissä). Peli: 3 rullaa / 1 voittolinja, panos **1 kolikko**. Rullanauha 20 merkkiä/rulla painoilla 🍒7 · 🍋5 · 🔔4 · 🍔2 · 💎2 → kolmikko **6,85 %**, pari **35,3 %**; maksut 💎×35 🍔×20 🔔×12 🍋×7 🍒×4 + pari = panos takaisin → **RTP 78,49 %** (validoitu 1 M simulaatiolla: 78,4–78,7 %). Tulos arvotaan heti (`pickSymbolId()`+`pickSlot()`), animaatio vain esittää sen: vapaa pyörintä → hidastus alkaa kun `distToTarget(pos, slot) <= DECEL_DIST` (4) → lineaarinen jarrutus (`SPIN_SPEED 15`, `STOP_DELAYS [0.35, 0.75, 1.15]`, `MIN_DECEL 0.22`); spin lukittu pyörinnän ajaksi. Canvas **640×400**, JS-skaalaus `min(availW/640, availH/400)` (pysty + vaaka); HUD ja napit DOM:issa: `Space`/`Enter` = pyöräytä, `Esc`/`Backspace` = poistu, kankaan napautus = pyöräytä, `#spin-btn` harmaantuu kun kassa < 1. Standalone-testitila: `?coins=N`, `?debug` (100 kolikkoa + DEBUG-teksti), `inIframe = window.parent !== window` → ilman parentia ei lähetetä viestejä. **postMessage-protokolla (valmis pelin puolelta):** katu→peli `{type:'fruitSync', coins}`; peli→katu `{type:'fruitBet'}` (−1), `{type:'fruitWin', coins:n}` (+n), `'RETURN_TO_STREET'` (poistuminen estetty kesken pyörinnän). Validointi ajettu `vm`+stub-tekniikalla (skriptit `%TEMP%\_rtp_test.cjs`, `_smoke_test.cjs`, `_layout_test.cjs` – **ei repossa**): syntaksi OK, 0 piirtoa kankaan ulkopuolelle, ei ajonaikaisia virheitä 3600 framessa, saldo ei mene miinukselle, bet/win-viestit täsmäävät. **Vaihe 2 = kytkentä pääpeliin (ks. "Seuraavaksi").**

**v4.10** | 19.9.2026 | Pimeiden ikkunoiden syvennysefekti 3 rivin taloihin (b2/b4/b6, `flatWindows`): kehyksen poiston jälkeen ikkunat katosivat seinään (täyttö `#0a0a15` vs runko ~`#1a1a2e` = liian vähän kontrastia, kehys `#2a2a3e` oli ainoa kirkas raja). Pimeän ikkunan haara jaettu: `flatWindows` → täyttö tummempi `#05050d` + 1 px tumma ylävarjo `rgba(0,0,0,0.35)` ikkunan sisäreunassa (`wx, wy, 10, 1`) + 1 px vaalea alaparre `lightenHex(bodyC, 0x06)` ikkunan alapuolella (`wx, wy+14, 10, 1`) → ikkuna lukee syvennyksenä (upotus); muut talot → entinen `#0a0a15` + kehys `#2a2a3e`. Valaistuihin haaroihin (houseLit ja `litWindows`) ei koskettu – ne saavat vain täytön + hehkun. Huom: nämä kolme taloa ovat `smallHouseLights`-taloja, joten potku sytyttää kaikki 6 ikkunaa kerralla → valaistuna ilme on näyttävä (tämä oli käyttäjän havainto). Versio v4.09 → v4.10.

**v4.09** | 19.9.2026 | Ikkunakehykset pois taaimmaisista kadun taloista (3 ikkunariviä = skaala 90 % = b2/b4/b6). `drawBuildings()` sai talokohtaisen lipun `flatWindows` (= `buildingWindowRows(b) <= 3`); kun se on tosi, ikkunan vaalea ulkokehys jätetään piirtämättä kaikissa kolmessa haarassa (valaistu talo, `litWindows`-ikkuna, pimeä ikkuna `#2a2a3e`) → `strokeRect(wx, wy, 10, 14)` on nyt `if (!flatWindows)`-ehdon sisällä. Täyttö, hehku-gradientti, siluetti, oven "ei-ikkunaa"-alue, `wx/wy`-koordinaatit ja `litWindows`-kirjanpito (`collectAllWindows`) ennallaan → ikkunat näyttävät pienemmiltä ja painuvat seinään, mikä korostaa 3D-kerroksisuutta (5/4/3-riviset talot erottuvat). Taustataloihin (`drawBackdropBlock`) ei koskettu – niissä ei ole kehyksiä. Versio v4.08 → v4.09.

**v4.08** | 19.9.2026 | Mopo (motorcycle) piirretty uusiksi: harmaan laatikon tilalle punainen runko (`#D32F2F`) + tumma astinlauta + musta penkki ja KUSKI, joka istuu penkillä kerrostettuna (runko/penkki → kuski → etukate+ohjaustanko kuskin päälle → syvyys). Kuski käyttää pelaajan värejä (paita/lippis `#3366cc`, lippa `#224488`, housut `#16265c`, iho `#ffcc99`, hiukset `#553300`), silmä katsoo eteen, käsi ojennettuna tankoon, jalat astinlaudalla. Tuuliefekti (`vib`, 1 px pään tärinä) poistettu luonnottoman näköisenä – kuskin pää/lippis staattinen. Piirto edelleen `drawVehicle()`:n `motorcycle`-haarassa; `dir`-peilaus ja ajovalokeila (rivit 3391–3410) ennallaan. Versio v4.07 → v4.08.

**v4.07** | 19.9.2026 | Syvyysefekti taloihin: matalammat kerrostalot skaalataan hiukan pienemmiksi kaukaisuusvaikutelman parantamiseksi. Uudet apufunktiot `buildingWindowRows(b)` = `Math.ceil((b.h - 60) / 32)` ja `buildingScale(b)`: 5 riviä = 100 % (b0/b3/b5/b8), 4 riviä = 95 % (b1/b7), 3 riviä = 90 % (b2/b4/b6). `drawBuildings()` käärii kunkin talon piirron (runko + ikkunat + `drawCornice`) canvas-transformiin, joka skaalaa pohjan keskipisteen `(b.x + b.w/2, GROUND_Y)` ympäri (`ctx.save(); translate(cx,GROUND_Y); scale(s,s); translate(-cx,-GROUND_Y); …; ctx.restore()`). Ovet skaalautuvat `drawDoor()`:ssa samalla `buildingScale(bldg)`-arvolla (ankkuri pohjan keskipiste `(dc.x, GROUND_Y)`), jolloin ovetkin pienenevät taaimmissa taloissa; oven x-keskipiste ja `doorCenter()` pysyvät ennallaan, joten potku/törmäyslogiikka (`DOOR_RADIUS`) ja lamppujen suuntaus säilyvät. Ikkunavalokirjanpito (`litWindows`/`collectAllWindows`) toimii yhä, koska se ja piirto käyttävät samoja skaalaamattomia koordinaatteja. Versio v4.06 → v4.07.

**v4.06** | 19.9.2026 | Kaukaisen kaupungin siluetti taustalle: uudet haaleat sinertävät taustatalot (`initBackdrop` / `drawBackdrop` / `drawBackdropBlock`) piirretään `render()`:ssä tähtien/taivaan/pilvien **päälle** ja lähitalojen/puiden/lamppujen **taakse** (kutsu ennen `drawBuildings()`). Parallaksi `BACKDROP_PARALLAX = 0.4` → `drawBackdrop(camX * (1 - 0.4))`, tausta liikkuu 40 % kameran nopeudesta (PC: `camX = 0` → staattinen). Yhtenäinen skyline (talot kiinni toisissaan, `w` 14–35, `h` 50–115 = **50 % koossa**, `BACKDROP_SCALE = 0.5` – hillitty ja kaukainen), 4 kattomallia (tasakatto/porrastus/harja/kattolaite), tumma ikkunaristikko 5×7 (`#0f1424`, ei glow'ta) + 1 himmeä lämmin ikkuna joka 3. taloon (`rgba(255,214,150,0.10)`), ohut reunalista (`band`) + ilmaperspektiivihuntu horisontissa. Generoidaan kerran `init()`:ssä (`initBackdrop()`, ei randomia per frame → ei välkyntää). Tyvi `BACKDROP_BASE_Y = GROUND_Y + 4` jää aina kiveyksen alle. Versio v4.05 → v4.06.

**v4.05** | 19.9.2026 | Kaksi Vaihe 3:n efektiä poistettu liioiteltuina: **potkun viuhka-VFX** (valkoiset/harmaat kaaret `))` + nopeusviivat) poistettu kokonaan `drawPlayer`in potkuhaarasta, ja **käsien heilunta** poistettu – `armPhase`-vaiheistus pois, kädet piirretään lepoasentoon (`backArmX = px+3`, `frontArmX = px+pw-6`) ja ne seuraavat vain vartalon `bobY`-liikettä (hengitys/kävelyn 1 px). Potkun ennakointi, nojaus (`kickLean`), hit pause ja dynaaminen reunavalo säilyivät ennallaan. Versio v4.04 → v4.05.

**v4.04** | 19.9.2026 | Hahmon animaatio-juice (Vaihe 3): **hengitys** – paikallaan seisoessa ylävartalo laskee 1 px ~2,4 s syklillä (`Math.floor(animClock/36) % 4 >= 2`), ei liikkeen/potkun aikana. **Käsien vastaheilunta** – `armPhase` (walkFrame 1 → +1, 3 → −1) siirtää takakäden eteen/ylös ja etukäden taakse/alas (±2 px x ja y) → vastakkain jalkojen kanssa. **Poton ennakointi** – `ANTICIP = 0.2`: ensimmäiset 20 % potusta jalka vedetään taakse (`swing = -0.35·kp/0.2`), sitten heilahdus `sin`-käyrällä; ylävartalo nojaa 1 px taakse ennakossa ja eteen osumassa (`kickLean` + `ctx.translate`). **Viuhka-VFX** – kaksi valkoista kaarta (r 16 ja 21, kulmat 0.45–1.45 / 0.60–1.25) + 2 nopeusviivaa jalan radalle, alpha `0.45·sin(sp·π)`. **Hit pause** – `const HIT_PAUSE = 2`, `hitPauseTimer` pysäyttää `update()`:n alussa koko maailman 2 frameksi; asetetaan talon 0 oven, pienien talojen ovien ja lamppujen osumahaaroissa (ei ovista sisään kävelyssä eikä törmäyksissä). **Dynaaminen valo** – lähin *palava* lamppu alle 70 px antaa ohuen lämpimän reunavalon (`rgba(255,221,136, 0.35·(1−d/70))`) vartalon ja pään valonpuoleiselle reunalle (lasketaan lokaalikoordinaateissa → kääntyy peilauksen mukana). Lisäksi silmän vilkahdus siirretty `Date.now()`:stä `animClock`-kelloon (100 ms / 3,6 s). Versio v4.03 → v4.04.

**v4.03** | 19.9.2026 | Hahmon pikseliviilaus (Vaihe 1+2, `drawPlayer`): **silmä** 2×2 `#2b2118` kulkusuunnan puoleisella reunalla (+ vilkahdus 90 ms / ~3,6 s `Date.now()`-pohjaisesti), **katseen pystysuunta** uusi `player.lookY` (update asettaa `moveY`:stä, piirto siirtää silmää ±1 px), **lipan varjo** `rgba(0,0,0,0.22)` rivillä py+5, **kasvojen takaosan varjo** `rgba(0,0,0,0.10)` 2×4 px, **leuan/niskan varjo** `rgba(0,0,0,0.22)` 2 riviä py+12 alkaen (pään ympyrä peittää paidan yläreunan → varjo vasta pään alareunan tasolle), **vyötäröraja** `rgba(0,0,0,0.20)`, **selän varjokaista** `#2b57ab` + **etureunan valokaista** `#4a7de0`, **housut** `#224488` → tumma laivastonsininen `#16265c`, **kengät** `#331100` → `#221008` + valojuova `#3a2a1c`, **hihansuut** `#254a9c` + **kädet** (iho `#ffcc99` / rystyt `#e8b487`). Uusi **maakosketusvarjo** (2 kerrosta ellipsiä: 0.30 alpha rx 10 seisoen / 11 kävellen + 0.45 alpha sisäkerros) piirretään ennen peilausta ja ennen knockedDown-haaraa. Lisäksi `ctx.imageSmoothingEnabled = false` `init()`:iin. Versio v4.02 → v4.03.

**v4.02** | 19.9.2026 | Puut huojuvat tuulessa: `drawBareTree(cx, baseY, h, swayX)` – tyvi ankkuroitu maahan, siirto kasvaa korkeuden mukaan (`rel^1.5`), joten puu taipuu eikä kaadu jäykkänä. `drawTrees()` laskee huojunnan pilvien tuulesta (`windDir`/`windSpeed`): hidas puuska (periodi ~17s) + huojunta (1.2–1.5 rad/s), amplitudi latvassa 0.6–2.8 px (kova tuuli huojuttaa enemmän), lepoasento tuulen suuntaan. Puille omat `phase`-arvot → eivät huoju synkassa. Siluetin muoto ennallaan (vain x-siirto) → ei välkyntää. Versio v4.01 → v4.02.

**v4.01** | 19.9.2026 | Sähkökaappi: toinen identtinen ilmentymä talon 7 (buildings[6], x 560–610, matala h 145) vasemmalle seinälle x 560 (2px rako oveen, ikkunat kaapin yläpuolella – sama suhde kuin talossa 3). `electricCabinet` → `electricCabinets`-taulukko (2 kpl); törmäysloopissa `for (const cab of electricCabinets)` + `if (player.knockedDown) break;` (logiikka muuten identtinen); drawElectricCabinet piirtää molemmat samalla koodilla (vilkkuva valo synkassa). Versio v4.00 → v4.01.

**v4.00** | 19.9.2026 | Aloituspopupin (showSpawnHint: 'Liiku kadulla, potki kaikkea…') näyttöaika +2s → 4500 ms. showNotification sai valinnaisen 2. parametrin `durationMs` (oletus 2500 ms) → kaikki muut popupit (kolikko, avaimet, ovet, varoitus) ennallaan 2.5 s + fade 0.5 s. Vain showSpawnHint kutsuu arvolla 4500.

**v3.99** | 19.9.2026 | Peliohjeet poistettu HUD-palkista (street.js updateHUD + index.html #hud-bar tyhjä). Ohje näytetään nyt pop-upina (sama kuin kolikkoilmoitus, showNotification): showSpawnHint() kutsutaan init()ssä vain kun tila on oletus/0 (ensimmäinen lataus tai kuoleman reset), teksti 'Liiku kadulla, potki kaikkea, mutta omalla vastuulla. Saattaa asukkaat hermostua! Ja muista Syödä!'. HUD näyttää nyt vain statuksen (avaimet/kolikot/hampurilaiset).

## ✅ Hahmon viilaus valmis (Vaiheet 1–3)

- **Vaihe 1 (v4.03):** silmä + `lookY`-katse, lipan/kasvojen/leuan varjot, kylkivarjostus, housujen kontrasti, hihansuut + kädet
- **Vaihe 2 (v4.03):** maakosketusvarjo (2 kerrosta), `imageSmoothingEnabled = false`
- **Vaihe 3 (v4.04):** hengitys, potkun ennakointi + nojaus, hit pause, dynaaminen lampunvalo — *käsien vastaheilunta ja potkun viuhka-VFX poistettu v4.05:ssä liioiteltuina*

### Havainnot, jotka kannattaa muistaa
- Pään ympyrä (arc r=7, keskipiste `py+6`) peittää paidan ylimmät rivit (`py+10..py+12`) → pään/kaulan varjostus vasta pään alareunan (`py+13`) tasolle.
- `handleAction()` palaa heti osumasta → hit pause asetetaan haaroissa, ja `actionJustPressed` nollataan joka framessa `loop()`:n lopussa (ei tuplapotkua).
- `KICK_DURATION` ja törmäyslogiikka ennallaan – ennakointi/viuhka ovat vain piirron aikakäyrää (`ANTICIP = 0.2`).
- Testityökalu: `street.js` renderöityy headless (Node `vm` + Proxy-canvas-stub, rAF ohjattavissa) → `drawPlayer`-operaatiot ASCII-rasteriksi. Ei jätetty repoon – kannattaa tehdä uudelleen vastaaviin visualisointeihin.
- Headless-validointi (v4.09–v4.10): sama vm+canvas-stub -tekniikka laskee piirrot per talo. Ikkuna = ainoa `10×14` suorakaide (hehku `22×24`, partikkelit `4×4`); syvennysefekti = `10×1` suorakaiteet (ylävarjo `rgba(0,0,0,0.35)`, alaparre `lightenHex(bodyC,0x06)`); talo tunnistetaan `wx`-x-alueesta. Rungon `bodyColor` luetaan piirrosta (`fillRect` talon mitoilla), koska `buildings` on IIFE:n sulkeumassa eikä globaali. Skripti Templissä (`%TEMP%\street-winframe-test.cjs`), ei repossa – tarkistaa 25 siemenellä: kehykset vain ≥4 rivin taloissa, syvennys vain 3 rivin taloissa.

## 🚧 Mahdollisia jatkoideoita (ei tehty)

- Potkun osumaan 1 px screen shake (voi tuntua CRT-teeman vastaiselta)
- Pää ja nyrkit "pixel-perfect" recteinä `arc()`-ympyrän sijaan
- Hahmon hengityksen syvyys 2 px tai hitaampi sykli, jos 1 px tuntuu liian pieneltä

## 🔒 Lukitut osa-alueet

- **Dialogi-tekstit:** Kaikkien pelien overlay/intro/viestit **LUKITTU**. Ei muutoksia ilman erillistä lupaa.
- **Pelien välinen logiikka:** Koko polku testattu läpi järjestyksessä (DG1 → DG2 → Blue Mäx). Avaimet, postMessage-kutsut, paluu kadulle — **LUKITTU**. Ei muutoksia ilman erillistä lupaa.
- **KAIKKI tekstit:** HUD-bar, notifikaatiot, lamppujen labelit, ovet, dialogit, overlay-tekstit — **LUKITTU**. Ei mitään tekstimuutoksia ilman erillistä pyyntöä.
- **Inventaario:** `inventory`-objektia (`coin`, `coinCount`, `hamburgerCount`) ei muokata ilman erillistä pyyntöä. Inventaario on lukittu.

## 🔜 Seuraavaksi (odottaa valintaa)

- **🎵 Jukebox (suunnitteilla): biisit peliin – masterit eivät gitissä.** Biisien masterit (`.mpeg`) ovat repon ulkopuolella: **`D:\AI\Knived`** (4 kpl: `Knived_Our_song_2013`, `Knived_Unafraid_2013`, `Knived_Unafraid_instrumental_2013`, `Knived_unafraid`). `.gitignore` estää nyt vahingossa tapahtuvat `*.mpeg`/`*.mp4`-committaukset. **Pakatut `.mp3`-versiot tarvitaan repoon**, koska GitHub Pages palvelee vain repon tiedostoja (nykyinen `knived_unafraid.mp3` = 2,0 MB / 128 kbps / −13,0 LUFS, ks. v4.16). Käsittely: `ffmpeg -af volume=-7dB -ar 44100 -ac 2 -b:a 128k` → tavoite ≈ −13 LUFS ja ≤ ~2–3 MB / biisi (repon koko). Nykyinen soitinsykli (kertasoitto + 30–90 s tauko, volume 0.05) on parametrejä, mutta jukebox-ominaisuus koskee `audio.js`:ää (pääsivun koodi) → **+0.01**.


- **🍒 Hedelmäpeli v4.11 – käytännön pelitestaus selaimessa** (ilmainen pyöräytys + jäädytyslaskuri + talo 7:n ovi). Testaa: `?coins=0` (ilmainen), `?debug`, poistu ja palaa < 120 s (ei ilmaista).
- **Nälkäajastin jäihin iframen ajaksi** (`street.js`, suojattu → vaatii erillisen luvan): `hamburgerTimer` ei tikitä kun `#game-iframe-overlay` on `active` → estää näkymättömän kuoleman ja kesken pyöräytyksen hukkuvan panoksen.
- **Hedelmäpelin avoimet säädöt:** RTP-preset (74 / **78,5** / 85 %), panosvalitsin 1/2/5, symboligrafiikan hienosäätö – päätökset odottavat.

- **🍒 Hedelmäpeli Vaihe 2 – kytkentä pääpeliin** ✅ *edellytykset täyttyvät: debug-kierros 1 valmis 19.9.2026 (peli käynnistyy DOMContentLoadedista, protokolla validoitu headless-testillä)* (käyttäjän hyväksymä ovi: `buildings[6]` = "talo 7", x 560–610, h 145 = 7. ovi vasemmalta, heti Blue Mäxin talon jälkeen):
  1. `handleAction()`: oma oviblokki `buildings[6]`:lle **ennen** geneeristä potkusilmukkaa (rivi ~1138): `doorCenter(buildings[6])` + `DOOR_RADIUS` → `enterGame('fruitgame/game_main.html')` → ohittaa talon potku- ja 1/5-kolikkohaaran. Ei vaadi lamppua eikä avainta (aina auki).
  2. `enterGame(url)`: `iframe.onload`-haaraan saldon synkkaus vain fruitgame-urleille: `iframe.contentWindow.postMessage({ type:'fruitSync', coins: coinCount }, '*')`.
  3. `window._streetReturn`: uudet haarat `e.data.type === 'fruitBet'` (−1 kolikko) ja `'fruitWin'` (+n, clamp) → `GameState.save(state)`, `updateHUD()`, ja saldon echo takaisin peliin. Vanhat viestit (`RETURN_TO_STREET`, `KEY_COLLECTED`, `BM_KEY_COLLECTED`, `BOULDER_KEY_COLLECTED`, `COIN_COLLECTED`) **ennallaan**.
  4. `index.html`: `#version-tag` → **v4.11**; memory-bank päivitys; (valinnainen) `docs/fruit-game-memo.md` (RTP-taulukko + protokolla).
  - **Säilyy ennallaan:** palkintohuone (COM-MANDO/dark room "To be continued…"), BAR-huone, kaikki avaimet. **Poistuu käytöstä:** talon `buildings[6]` potku-valot (`smallHouseLights[6]`) ja 1/5 kolikkopudotus – potkulotto jää taloihin `buildings[2]` ja `buildings[4]`; sähkökaappi (x560) ja v4.10:n pimeiden ikkunoiden syvennys jäävät visuaalisesti ennalleen.
  - Loki: `coinCount` pysyy kadun autoritatiivisena (`GameState` = totuus, `closeGame()` lataa sen); `inventory`-rakennetta ei muuteta; olemassa oleviin teksteihin (mm. COM-MANDO-label) ei kosketa ilman erillistä lupaa.
- **Odottavat käyttäjän päätökset (Vaihe 1:n UI-testauksen jälkeen):** RTP-preset (A 74 % / **B 78,5 % oletus** / C 85 %), panos kiinteä 1 vs. 1/2/5-valitsin, symboligrafiikan hienosäätö. Testaus: avaa `fruitgame/game_main.html` suoraan (`file://`) tai `http://localhost:8080/fruitgame/game_main.html`, parametrit `?coins=100` / `?debug`.
- Blue Mäx: TESTIMODE pois → palauta vihollisten ammunta normaalille 60% aggressiolle
- **Ääniefektit pääportaaliin:** ✅ Valmis v3.28 – täysi bändisoundi (rummut + basso + kitara + melodia, audio.js)
- **Pääsiäismunat Dig Däshiin**

## ⚠️ Huomiot

- **🔒 KADUN TALOUS (mekaniikat – luvut ovat kattoja, eivät toteudu pelissä):** Käyttäjän havainto 19.9.2026: paperilaskettu tulotaso ei päde, koska **kaikkia kolikoita ei ehdi poimia** ja onnettomuudet syövät hampurilaisia. Todelliset mekaniikat: katukolikko näkyvissä vain **10 s** (`despawnTimer 600`) → **30 s tauko** (`despawnCooldown 1800`) → uusi satunnaispaikka (x 0–780, y 310–380); kadun ylitys kestää ~10,6 s → kolikko ehtii usein **kadota nenän edestä**. Keräysalue osuu autokaistoille (kaistat y 328 ja 340, auto 30 px korkea; turvassa vain teräsaidan juuressa `player.y ≥ 347` sekä kaistojen välissä ±5 px) → kerääminen vaatii riskinottoa. Potkukolikko: 1/5 × 30 s cooldown. **Menot:** nälkä 1 🍔 / 40 s (r. 831) **+ auto-osuma −1 🍔** (r. 1042) **+ sähköisku −1 🍔 ja 10 s tainnutus** (r. 817) **+ kukkaruukun osuma −1 🍔 ja 10 s tainnutus** (r. 894) → onnettomuudet pakottavat ostamaan lisää ruokaa (1 🍔 = 1 kolikko BAR:sta, katto 10). Hedelmäpelin maksettu pyöräytys on aina miinus-EV (RTP 78,49 %, −0,215/pyöräytys); ilmainen pyöräytys 1/120 s. **Tasapainoa ei todisteta laskemalla – se testataan pelaamalla.** Säädönupit: RTP-preset, ilmaispyöräyksen jäädytys, nälkäajastin, kolikon 10 s / 30 s sykli, potkukolikon 1/5 + 30 s, onnettomuusmenetykset (−1 🍔), BAR-katto 10.
- **Pääportaali:** `position: absolute`, `opacity: 0.65`, landscape overlay, D-pad + ⚡
- **🚪 Ovikynnykset (v4.13 – suunnittelusääntö):** Talot ovat **kiinni kadun varressa** (ei etupihaa) → ovikiveys ei saa valua kadulle eikä olla ristiriidassa vaakakiveyksen kanssa. Siksi jokaisen oven edessä on vain **yksi pieni kivirivi** kynnyslaatan alla: kiveys rajoittuu `GROUND_Y+7…+14` (y 317–324), puolileveys = ovi + 5 px. Autokaistat (y 328 ja 340) ovat vapaita. Säädönupit: `THRESH_RINGS` (d/w/n), `THRESH_TOP_Y`, `THRESH_DIP`, `THRESH_DETAILS`, `THRESH_LIGHT`, `KERB_GAP_EXTRA`, kynnyslaatan korko (`slab.h`). Testi vahtii rajoja (`%TEMP%\street-threshold-test.cjs`: alaraja ≤ `GROUND_Y+14` ja puolileveys ≤ ovi+5 → kaatuu, jos kiveys valuu kadulle).

- **🚪 Oviukko (v4.15 – säädönupit):** `AVENGER_CHANCE 0.12` (1/8) · `AVENGER_COOLDOWN 1800` (30 s) · `AVENGER_SPEED 2.0` (> pelaaja 1.225 → **ei väistettävissä**) · `AVENGER_STUN 600` (→ 360 = 6 s jos tuntuu liian kovalta) · `AVENGER_FREEZE 180` (**3 s jäädytys kontaktista**, 120 = 2 s / 300 = 5 s) · `AVENGER_TELEGRAPH 21` (~350 ms varoitus) · `AVENGER_HIT_R 18` · `AVENGER_KIND 'twin'` (koira myöhemmin 70/30). Oviukko tulee ovesta **kynnykseltä** (ei putoa), jäädyttää maailman 3 s (vinjetti + välähdys + tärinä + tähdet), kosahtaa sitten (tainnutus + 1 🍔) ja palaa ovelle; iframen aikana jäissä. Testi vahtii: kynnys-y (ei ikkunakorkeus), nopeusero, **jäädytyksen kesto + ettei pelaaja liiku jäädytyksessä**, tasan 1 🍔, ei tuplaosumaa, paluu + `null`, portti `%TEMP%\street-avenger-test.cjs`.
- **🎵 Taustamusiikki (v4.18 – säädönupit):** tiedosto `knived_unafraid.mp3` = **koko biisi 2:08** (128 kbps / 44,1 kHz / −13,0 LUFS) – pysyy repossa täytenä, koska se tulee **jukeboxiin** · soitetaan vain **alku**: `SONG_PLAY_LIMIT 30000` + `SONG_FADE_OUT 600` (volume-ramppi 12 × 50 ms → 0, perustaso `MUSIC_VOLUME 0.05`) · ajastin `setTimeout(fadeOutSong, songPlayLength() − SONG_FADE_OUT)` = **29 400 ms**, missä `songPlayLength() = Math.min(SONG_PLAY_LIMIT, kesto + 1 s)` (turvaraja lyhyelle tiedostolle) · syntikka-fallback ja varasuoja `SYNTH_PLAY_DURATION 30000` · kappale alkaa aina alusta (`currentTime = 0`), `ended` = turvaverkko · tauko 30–90 s (`getSilenceDuration`) → **sykli 60–120 s** · `stopMusicLoop()` palauttaa volumen; `silencePhase()`/`stop()` siivoavat `fadeTimer`in. Testi `%TEMP%\audio-music-test.cjs` (43 tarkistusta): alku 30 s + häivytys + tauko, ei x2-toistoa, lyhyt tiedosto ei ylitä kestoa.
- **Sähkökaapit:** laskettu 2px (pohja `GROUND_Y-2`), pienennetty 20 % (8×14), vilkkuva valo puolet hitaampi (`/520`).
- **Notifikaatiot:** Vain ovi/kolikko, `showNotification(text, durationMs = 2500)` → lukuaika 2.5s + fade 0.5s. Aloitusohje (showSpawnHint) 4500 ms (v4.00). Muiden popuppien aikoja ei pidennetä.
- **Blue Mäx:** ✅ v3.10 – Musta ⅓-leveä mittari-HUD (polttoainepalkki + pommit + elämät + korkeus), ei pisteitä. Kentän ulkopuolella alt≤1 → STALL-kuolema. Ohjaus pois ST.TO:n ajalta. Viholliset 100% teholla. Polttoaineen loputtua syöksy + wrap-around, 3-kerroksinen räjähdys, mobiili-HUD overlayna. B=pommi, G=KK, L=laskeudu. Ilmapallo 8 taloa + 60s. Tankkaus. Kaikki talot bonus +5000.
- **Dig Däsh:** Debugissa avain heti | **Dig Game:** Buildaus poistettu, `game_main.html` käytössä suoraan
- **🏷️ Pelinimi (20.9.2026):** `digGame2/` = **Dig Däsh** (vanha nimi "Boulder Däsh" poistettu kaikista näkyvistä teksteistä). **Sisäiset nimet jäivät:** `boulderKeyCollected`, `BOULDER_KEY_COLLECTED`, `TILE.BOULDER`, `SCORE_BOULDER_FALL`, `drawBoulder`, kansionimi `digGame2/`, tiedostot `docs/bd-*.md` ja `boulder_dash.html` (kuollut viittaus `.clineignore`issa). Älä nimeä niitä uudelleen ilman erillistä pyyntöä – ne ovat localStorage-/postMessage-rajapintaa (lukittu, sääntö 02). **Käyttäjän linjaus 20.9.2026:** `boulder` muuttujissa/tunnisteissa ei ole tavaramerkkiasia – se **saa olla**, vain näkyvät tekstit muutetaan. Varmistettu skannauksella (`%TEMP%\boulder-visible-scan.cjs`): näkyviä "Boulder"-merkkijonoja 0 kpl.
- **Versio:** v3.3 GitHub Pagesissa `https://teppoaland.github.io/pimeakatu/`